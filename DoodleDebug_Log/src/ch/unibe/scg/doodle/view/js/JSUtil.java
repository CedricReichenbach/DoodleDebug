package ch.unibe.scg.doodle.view.js;

import java.net.URL;

import ch.unibe.scg.doodle.util.ToDoException;

public class JSUtil {
	public static URL getJSURLFromFile(String filename) {
		throw new ToDoException();
		// TODO: Either rewrite this without Eclipse or directly include JS in
		// HTML code
		// return FileLocator.resolve(JSUtil.class.getResource(filename));
	}
}
